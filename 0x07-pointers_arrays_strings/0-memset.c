#include "main.h"
/**
 * _memset - fills memory with a constant bytes 
 * @s: pointer
 * @b: character vriable
 * @n: unsigned int
 *
 */
char *_memset(char *s, char b, unsigned in n)
{	

	unsigned int i;
	for (i =0; 
			while (i < n)

{     
  s[i] = b;
   return (s);
   }
